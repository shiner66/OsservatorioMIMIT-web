# Dipendenze runtime di OsservatorioMIMIT-web.
# Versioni minime specificate con >=; per build di produzione riproducibili
# usa `pip freeze > requirements.lock` e installa da quello.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pandas>=2.2
pydantic>=2.7
python-multipart>=0.0.9
